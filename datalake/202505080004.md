---
title: Filtering messages in Python
draft: false
tags:
  - python
  - filtering
  - code
---

# Filtering messages in Python

Look at the following snippet:

```python
def filter_messages(messages):
    filtered = []
    removed = []
    
    for message in messages:
        words = message.split()
        clean_words = []
        count = 0

        for word in words:
            if word == "dang":
                count += 1
            else:
                clean_words.append(word)

        filtered_message = " ".join(clean_words)
        filtered.append(filtered_message)
        removed.append(count)

    return filtered, removed
```

