---
title: Playing with sets in iteration
draft: false
tags:
  - python
  - code
  - sets
  - iteration
---
# Playing with sets in iteration

[Sets](https://docs.python.org/3/tutorial/datastructures.html#sets) are _like_ Lists, but they are _unordered_ and they guarantee _uniqueness_. Only _ONE_ of each value can be in a set.

In this case, we are taking the inputs and sorting them for uniqueness.

## The code:
```python
def remove_duplicates(spells):
    seen = set()
    unique_spells = []
    for spell in spells:
        if spell not in seen:
            seen.add(spell)
            unique_spells.append(spell)
    return unique_spells
```

### How it works:
